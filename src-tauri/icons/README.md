# Icônes

Placeholder. Avant le premier `cargo tauri build`, déposer ici :

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

Génération rapide depuis un seul PNG carré :

```bash
npm run tauri icon path/to/source-1024.png
```
